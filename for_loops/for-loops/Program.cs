// A for loop that counts up
for (int i = 0; i < 10; i++)
{
	Console.WriteLine(i);
}

// A for loop that counts down
for (int counter = 10; counter > 0; counter--)
{
	Console.WriteLine(counter);
	//Add a 1 second delay 
	Thread.Sleep(1000);
}